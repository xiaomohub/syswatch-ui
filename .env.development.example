# 本地开发：Vite 将 /api 代理到后端，见 vite.config.ts server.proxy（默认 http://localhost:8080）。
# 首页大盘 GET/POST /api/dashboard/overview 须由该后端实现；上游 Prometheus/VM 地址在后端配置，前端无需填写。

# 可选：不启主后端时，可单独跑参考服务 mock 大盘接口：
#   npm run dashboard-api
# 然后把 vite.config.ts 里 proxy target 改为 http://127.0.0.1:8090（或你设的端口）。
# 该脚本需要查询根地址：
# SYSWATCH_VM_QUERY_URL=http://127.0.0.1:8428
# VM 集群示例：
# SYSWATCH_VM_QUERY_URL=http://10.0.0.1:8481/select/0/prometheus
